[project]
name = "w2-prompts"
version = "0.1.0"
description = "W2 prompt 渲染与测试"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0",
    "openai>=2.48.0",
    "python-dotenv>=1.2.2",
    "pydantic>=2.13.4",
]

[tool.uv]
package = false
